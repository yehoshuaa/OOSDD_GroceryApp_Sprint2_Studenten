## Git branches
We gebruiken een simpele Gitflow indeling:

- `main`: stabiele versie  
- `develop`: werkversie  
- `feature/*`: aparte branch per use case (bijv. `feature/UC06-login`)

Werkwijze:
1. Maak een nieuwe feature branch vanaf `develop`
